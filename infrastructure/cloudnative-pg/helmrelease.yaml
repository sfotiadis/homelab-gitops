apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: cnpg-system
spec:
  interval: 30m
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.28.2
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: flux-system
  values:
    crds:
      create: true
    replicaCount: 3
    monitoring:
      # TODO: reactivate once kube-prometheus-stack is installed and configured
      # podMonitorEnabled: true
      grafanaDashboard:
        create: true
        namespace: monitoring