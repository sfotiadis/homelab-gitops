apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenants
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: crulabs-k8s
  path: ./tenants
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
